# 更新记录 (Changelog)

## 1.1.0 — 2026-08-19

### 千期数据升级（100 期 → 1000 期）

- **数据拉取**：官方接口 `issueCount` 100 → 1000，一次拉取最近 1000 期开奖数据（2019134 ~ 2026095）；在线更新脚本 `update-data.mjs` 同步
- **插件版（DSH）**：`ssq` 工具 `window` 参数上限 100 → 1000，默认窗口 1000（trend / predict 均生效）
- **HTML 版**：统计窗口选择器新增 200 / 500 / 1000 期，默认 1000 期；页面「在线更新」接口同步 `issueCount=1000`
- **内置快照**：`data/ssq-history.json` / `history.json` 更新为 1000 期，离线状态亦可做千期完整分析
- **测试**：`test.cjs` 断言同步为千期，新增千期窗口次数守恒（红球 6000 次 / 蓝球 1000 次）与千期预测校验
- **文档**：README 同步千期口径；新增本更新记录

### 增量更新机制（历史保存 + 少量拉取）

- 历史 1000 期数据直接保存在本地（插件内置快照 / HTML 内嵌），作为分析基线
- 更新改为**增量合并**：只拉官方接口最新 200 期（`issueCount=200`）→ 与本地基线按期号去重合并 → 截取最近 1000 期；快照过期太久（合并出现缺口）时自动回退全量 `issueCount=1000` 重建
- 镜像源（约 156 期）作为官方不可达时的增量备用，合并后仍以基线补齐 1000 期
- 核心新增 `mergeDraws(baseline, fresh, max)`（插件版 `lib/ssq-core.js` 与浏览器版 `src/app.cjs` 双实现，测试校验一致性）：去重 / 排序 / 截取 / 新增期数统计 / 连续性检测（跨年衔接视为连续）
- 更新后数据显示「当前 N 期（最旧 ~ 最新）」，并标注数据来源与最新一期

### 预测页独立数据范围

- HTML 智能预测页新增「**预测数据范围**」下拉：最近 100 / 200 / 500 / 1000 期（默认 1000），选择后按所选期数统计并预测
- 预测窗口与趋势页窗口**相互独立**，可分别选择；预测结果显示所用期数范围（如「数据：最近 500 期，2024082 ~ 2026095」）
- **DSH 插件版同步**：`ssq` 工具 `predict` 通过 `window` 参数指定预测数据范围（10–1000，默认 1000），参数描述与工具说明明确档位（100/200/500/1000）；预测结果与 detail 均输出所用期数区间（如「基于最近 500 期，2023049 ~ 2026095」）

### 新增 5 个预测模型

- **模型预设扩展至 6 个**：新增 `ewma`（EWMA 近期加权，半衰期 20 期指数加权信号）、`miss`（遗漏均值回归，纯遗漏视角）、`expect`（期望偏差回补，次数低于期望的号码靠前，大数回归）；原有 balanced/cold/hot 保留
- **核心新增**：`computeStats` 增加 EWMA 信号（z 标准化）；`predict` 评分扩展为四信号加权（freq/rec/gap/ewma）；新增 `ensemble`（集成投票：6 模型各自推荐 Top6 红 + Top1 蓝，票数高者胜出，同票按平均概率）与 `structureStats` + `genStructureCombos`（组合结构统计与按结构选号）
- **HTML 版**：预测模型区新增 3 个预设单选；新增「⚖ 集成投票」「📊 组合结构预测」按钮与结构结果卡片（奇偶/大小/三区间/和值频率 + 最可能结构选号 5 注）
- **DSH 插件版**：`preset` 枚举扩展为 6 个；新增 `action=ensemble`、`action=structure`，输出与 detail 含投票/结构明细
- 双实现保持一致（test.cjs 校验），README 同步

## 1.0.0 — 2026-08-18

- 双色球助手插件（趋势追踪 / 智能预测 / 胆拖随机选号），含单文件 HTML 版
- 改造为标准 DSH 插件包：注册 `ssq` 聊天工具，声明 `dsh.bundle`，多源拉取（官方接口 → GitHub 镜像 → CDN）并内置快照兜底
